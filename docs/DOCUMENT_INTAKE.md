# 🛡️ Безопасный приём DOCX/XLSX

Статус: **базовый объём M2 реализован**  
Последнее обновление: **2026-07-12**

## Назначение

Первичная проверка защищает систему до сохранения документа как шаблона. Она отвечает на три вопроса:

1. можно ли безопасно прочитать пакет;
2. какие возможности Office требуют внимания;
3. какой следующий шаг допустим.

Проверка выполняется локально, без LLM и без запуска содержимого документа.

## Пользовательский процесс

```text
выбор DOCX/XLSX
      ↓
проверка имени, размера и ZIP
      ↓
проверка частей и ограничений
      ↓
поиск макросов и внешних связей
      ↓
русскоязычный отчёт
```

Возможные результаты:

| Состояние | Значение |
|---|---|
| **Структура прошла проверку** | Блокирующие особенности не найдены. Можно переходить к построению структуры документа. |
| **Файл принят с замечаниями** | Продолжение возможно, но пробное формирование и ручная проверка обязательны. |
| **Файл нельзя использовать** | Найдена блокирующая или повреждённая структура. Файл не сохраняется как шаблон. |

## Реализованные проверки

- расширение и безопасное имя файла;
- сигнатура ZIP;
- максимальный размер исходного файла;
- максимальное число частей;
- суммарный распакованный размер;
- размер отдельной части;
- подозрительно высокая степень сжатия;
- абсолютные пути и выход через `..`;
- обратные косые черты и управляющие символы;
- повторяющиеся и различающиеся только регистром имена частей;
- зашифрованные части;
- символические ссылки;
- неподдерживаемый способ сжатия;
- обязательные части DOCX/XLSX;
- макросы VBA;
- ActiveX;
- встроенные OLE-объекты;
- цифровые подписи;
- внешние связи и внешние книги;
- сложные схемы и пользовательские XML-данные.

## Ограничения по умолчанию

| Ограничение | Значение |
|---|---:|
| Размер исходного файла | 32 МБ |
| Число частей | 2048 |
| Суммарный распакованный объём | 256 МБ |
| Размер отдельной части | 32 МБ |
| Размер читаемой служебной XML-части | 4 МБ |
| Максимальная степень сжатия | 200:1 |

Ограничения задаются в `DEFAULT_INTAKE_LIMITS` и должны меняться только вместе с нагрузочными и отрицательными проверками.

## Прикладной интерфейс

```http
POST /api/v1/document-intake/inspect?fileName=Шаблон.docx
Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document
```

Тело запроса содержит двоичный файл. Ответ включает:

```json
{
  "data": {
    "fileName": "Шаблон.docx",
    "format": "docx",
    "decision": "accepted",
    "sha256": "...",
    "summary": {
      "entryCount": 3,
      "fileCount": 3,
      "compressedBytes": 412,
      "uncompressedBytes": 705,
      "externalRelationships": 0,
      "hasMacros": false
    },
    "issues": []
  },
  "correlationId": "..."
}
```

Машинные значения `accepted`, `accepted_with_warnings` и `rejected` переводятся интерфейсом в русские состояния. Они не должны показываться пользователю без пояснения.

## Граница безопасности

На текущем этапе файл:

- не выполняется;
- не передаётся локальной модели;
- не распаковывается в пользовательский каталог;
- не сохраняется как активный шаблон;
- не вызывает внешние связи;
- не запускает LibreOffice.

## Что остаётся в M2

- потоково прочитать и проверить фактические данные каждой части, а не только метаданные центрального каталога;
- проверить контрольные суммы всех частей;
- сохранить принятый исходник в неизменяемом карантине;
- построить Document IR для DOCX;
- построить Document IR для XLSX;
- присвоить устойчивые идентификаторы абзацам, фрагментам, таблицам, листам и ячейкам;
- добавить структурный предварительный просмотр;
- сохранить ручную координату изменяемого поля.

## Связанные требования

`TPL-001`, `TPL-002`, `TPL-003`, `TPL-004`, `TPL-014`, `TPL-015`, `SEC-002`, `SEC-009`, `SEC-012`, `AC-021`.
