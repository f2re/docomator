from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]


def replace_once(relative: str, old: str, new: str) -> None:
    path = ROOT / relative
    value = path.read_text(encoding="utf-8")
    count = value.count(old)
    if count != 1:
        raise RuntimeError(f"{relative}: ожидалось одно вхождение, найдено {count}")
    path.write_text(value.replace(old, new, 1), encoding="utf-8")
    print(f"updated {relative}")


replace_once(
    "README.md",
    '''1. создавать произвольные типы объектов: людей, аудитории, статьи, оборудование и другие записи;
2. импортировать до 1000 объектов одного типа из CSV или XLSX;
3. повторно загружать обновлённый список без создания дублей по устойчивому идентификатору;
5. создавать типизированные свойства и однородные группы;
4. безопасно загрузить DOCX/XLSX;
6. выделить изменяемый текст внутри абзацев DOCX или выбрать ячейки XLSX как поля;
7. задать безопасный русский формат числа, даты, даты-времени или `Да`/`Нет` и проверить несколько полей одной окончательной копией;
8. создать PDF-предпросмотр и активировать версию;
9. сформировать один сводный документ;
10. сформировать отдельный документ для каждого участника;
11. увидеть и исправить обязательные пропуски до запуска;
12. получить отдельный файл или ZIP-комплект;
13. повторить только неуспешные документы;
14. сохранить получателей электронной почты;
15. передать результат в разрешённую сетевую папку;
16. отправить результат через SMTP;
17. создать однократное, ежедневное или ежемесячное расписание;
18. автоматически доставить календарный выпуск через SMTP или сетевую папку;
19. увидеть новый результат в общем корпоративном хранилище;
20. после перехода или перезагрузки увидеть ход предпросмотра, формирования и доставки в едином центре операций;
21. рассчитать и подтвердить безопасную очистку объектов без ссылок;
22. проверить готовность всех обязательных компонентов на одном экране;
23. использовать ежедневные проверяемые резервные копии.''',
    '''1. создавать произвольные типы объектов: людей, аудитории, статьи, оборудование и другие записи;
2. импортировать до 1000 объектов одного типа из CSV или XLSX;
3. повторно загружать обновлённый список без создания дублей по устойчивому идентификатору;
4. создавать типизированные свойства и однородные группы;
5. безопасно загружать DOCX/XLSX;
6. выделять изменяемый текст внутри абзацев DOCX или выбирать ячейки XLSX как поля;
7. задавать безопасный русский формат числа, даты, даты-времени или `Да`/`Нет` и проверять несколько полей одной окончательной копией;
8. сохранять проверенную версию шаблона и при необходимости отдельно создавать PDF для визуальной проверки;
9. формировать один сводный документ;
10. формировать отдельный документ для каждого объекта;
11. видеть и исправлять обязательные пропуски до запуска;
12. получать отдельный файл или ZIP-комплект;
13. повторять только неуспешные документы;
14. сохранять получателей электронной почты;
15. передавать результат в разрешённую сетевую папку;
16. отправлять результат через SMTP;
17. создавать однократное, ежедневное или ежемесячное расписание;
18. автоматически доставлять календарный выпуск через SMTP или сетевую папку;
19. видеть новый результат в общем корпоративном хранилище;
20. после перехода или перезагрузки видеть ход предпросмотра, формирования и доставки в едином центре операций;
21. рассчитывать и подтверждать безопасную очистку объектов без ссылок;
22. проверять готовность всех обязательных компонентов на одном экране;
23. использовать ежедневные проверяемые резервные копии.'''
)

replace_once(
    "README.md",
    '''активный шаблон
+ зафиксированная аудитория N участников
→ N независимых DOCX/XLSX
→ отдельные файлы или ZIP''',
    '''активный шаблон
+ зафиксированный состав N объектов одного типа
→ N независимых DOCX/XLSX
→ отдельные файлы или ZIP'''
)
replace_once(
    "README.md",
    "Ошибка одного участника не блокирует остальные документы. После исправления данных можно повторить только проблемные строки.",
    "Ошибка одного объекта не блокирует остальные документы. После исправления данных можно повторить только проблемные строки."
)
replace_once(
    "README.md",
    '''активный шаблон и его поля
+ аудитория N участников
→ один DOCX/XLSX
→ строка на участника''',
    '''активный шаблон и его поля
+ зафиксированный состав N объектов одного типа
→ один DOCX/XLSX
→ строка на объект'''
)
replace_once(
    "README.md",
    "Если активный шаблон содержит проверенную повторяемую область, backend заполняет одну строку DOCX либо строку/непрерывный диапазон XLSX на каждого участника.",
    "Если активный шаблон содержит проверенную повторяемую область, сервер заполняет одну строку DOCX либо строку или непрерывный диапазон XLSX на каждый объект."
)
replace_once(
    "README.md",
    "В DOCX можно выбрать одну строку таблицы и отметить «Повторять эту строку для сотрудников».",
    "В DOCX можно выбрать одну строку таблицы и настроить её повторение для объектов выбранного типа."
)
replace_once(
    "README.md",
    "В сводном выпуске backend детерминированно создаёт по строке на участника из неизменяемого снимка",
    "В сводном выпуске сервер детерминированно создаёт по строке на объект из неизменяемого снимка"
)
replace_once(
    "README.md",
    "а весь точный состав входит в offline bundle.",
    "а весь точный состав входит в автономный комплект."
)
replace_once(
    "README.md",
    "Они и соседняя матрица совместимости входят в offline bundle с контрольными суммами",
    "Они и соседняя матрица совместимости входят в автономный комплект с контрольными суммами"
)

replace_once(
    "docs/OFFLINE_DEPLOYMENT.md",
    '''пространство → участники → аудитория → проверка шаблона
→ поле → пробное заполнение → PDF → активация''',
    '''пространство → тип объектов → данные или импорт → проверка шаблона
→ поля → пробное заполнение → сохранение шаблона
→ необязательная визуальная проверка PDF → выпуск документов'''
)
replace_once(
    "docs/RELEASE_NOTES.md",
    "5. получить PDF-предпросмотр через установленный LibreOffice и активировать неизменяемую версию;",
    "5. сохранить проверенную неизменяемую версию и при необходимости отдельно получить PDF-предпросмотр через установленный LibreOffice;"
)
replace_once(
    "docs/RELEASE_NOTES.md",
    "6. сформировать отдельный документ на каждого участника либо один сводный документ из пользовательской repeat-строки;",
    "6. сформировать отдельный документ на каждый объект либо один сводный документ из настроенной повторяемой строки;"
)
replace_once(
    "docs/RELEASE_NOTES.md",
    "9. продолжить сохраняемую операцию после перезагрузки страницы или перезапуска worker.",
    "9. продолжить сохраняемую операцию после перезагрузки страницы или перезапуска фонового обработчика."
)

print("documentation flow corrected")
