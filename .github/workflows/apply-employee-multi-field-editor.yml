name: Применить редактор нескольких полей сотрудника

on:
  pull_request_target:
    branches:
      - main
    types:
      - opened
      - reopened
      - synchronize
      - edited

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'feature/employee-multi-field-editor'
    runs-on: ubuntu-24.04
    timeout-minutes: 35

    steps:
      - name: Получить рабочую ветку
        uses: actions/checkout@v4
        with:
          ref: feature/employee-multi-field-editor
          fetch-depth: 0
          show-progress: false

      - name: Определить необходимость применения
        id: detect
        shell: bash
        run: |
          if [[ -f scripts/ci/apply-employee-multi-field-editor.py ]]; then
            echo 'apply=true' >> "$GITHUB_OUTPUT"
          else
            echo 'apply=false' >> "$GITHUB_OUTPUT"
            echo 'Редактор нескольких полей уже применён.'
          fi

      - name: Применить изменения интерфейса
        if: steps.detect.outputs.apply == 'true'
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 scripts/ci/apply-employee-multi-field-editor.py
          git diff --check

      - name: Определить версию Node.js
        if: steps.detect.outputs.apply == 'true'
        id: node
        shell: bash
        run: echo "version=$(cat .node-version)" >> "$GITHUB_OUTPUT"

      - name: Установить Node.js
        if: steps.detect.outputs.apply == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ steps.node.outputs.version }}
          cache: npm

      - name: Установить зависимости
        if: steps.detect.outputs.apply == 'true'
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Проверить репозиторий
        if: steps.detect.outputs.apply == 'true'
        run: npm run check

      - name: Проверить миграции
        if: steps.detect.outputs.apply == 'true'
        env:
          DOCOMATOR_DATA_DIR: ${{ runner.temp }}/docomator-employee-fields
        run: npm run migrate

      - name: Зафиксировать проверенные изменения
        if: steps.detect.outputs.apply == 'true'
        shell: bash
        run: |
          set -Eeuo pipefail
          rm -f \
            .apply-employee-multi-field-editor \
            scripts/ci/apply-employee-multi-field-editor.py \
            .github/workflows/apply-employee-multi-field-editor.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add --all
          git diff --cached --check
          git commit -m 'feat employee multi-field card editor'
          git push origin HEAD:feature/employee-multi-field-editor
