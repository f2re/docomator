name: Исправить счётчик идентификаторов DOCX

on:
  push:
    branches:
      - fix/template-structure-preview-csp
    paths:
      - .github/workflows/fix-docx-repeat-unique-count.yml
      - scripts/ci/fix-docx-repeat-unique-count.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/template-structure-preview-csp
          fetch-depth: 0
          show-progress: false
      - run: python3 scripts/ci/fix-docx-repeat-unique-count.py
      - shell: bash
        run: |
          set -Eeuo pipefail
          rm -f \
            scripts/ci/fix-docx-repeat-unique-count.py \
            .github/workflows/fix-docx-repeat-unique-count.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add --all
          git diff --cached --check
          git commit -m 'Исправить проверку уникальных идентификаторов DOCX'
          git push origin HEAD:fix/template-structure-preview-csp
