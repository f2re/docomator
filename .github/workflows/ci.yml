name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Build, test, docs, and shell
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read Node.js version
        id: node
        shell: bash
        run: echo "version=$(cat .node-version)" >> "$GITHUB_OUTPUT"

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ steps.node.outputs.version }}
          cache: npm

      - name: Install locked dependencies
        id: npm_ci
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node --version
          npm --version
          npm ci --ignore-scripts --no-audit --no-fund 2>&1 | tee npm-ci.log

      - name: Upload npm diagnostic
        if: steps.npm_ci.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: npm-ci-diagnostic
          path: npm-ci.log
          if-no-files-found: error
          retention-days: 3

      - name: Stop after failed dependency installation
        if: steps.npm_ci.outcome == 'failure'
        run: exit 1

      - name: Verify repository
        run: npm run check

      - name: Smoke-test migration on a fresh database
        env:
          DOCOMATOR_DATA_DIR: ${{ runner.temp }}/docomator-data
        run: npm run migrate
